@model List<EmployeeMVC.Models.EmployeeCls>

<a asp-action="Create" asp-controller="Employee" class="btn btn-success"> Enter Employee </a>

<ul>

    @foreach (var emp in Model)
    {
        <li>
            @emp.Name
        </li>
    }
</ul>

